import { Card, styled } from '@mui/material';

export const CardUtil = styled(Card)({
  backgroundColor: '#252627',
  color: '#fff',
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%,-50%)',
  padding: '40px'
});
